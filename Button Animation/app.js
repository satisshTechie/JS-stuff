const sign_up = document.querySelector('.signup_btn');
const Icon = document.querySelector('.fa-solid');

sign_up.addEventListener('click',signupButton);

function signupButton(){
    this.classList.add('Icon');
}